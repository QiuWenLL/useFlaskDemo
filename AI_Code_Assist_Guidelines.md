
# 📘 AI 编程协作规范（适用于 Copilot / Cline AI / VS Code）

> **目的**：在使用 AI 编程助手时，提升代码质量、可维护性，避免无控制的代码生成行为。

---

## 🔧 基本原则

| 编号 | 原则                    | 说明                                                         |
|------|-------------------------|--------------------------------------------------------------|
| R1   | **仅处理当前问题**     | AI 仅操作与当前提问相关代码，禁止跨范围自动更改            |
| R2   | **优先使用中文提示**   | 提高沟通清晰度，便于回溯和理解                              |
| R3   | **每次请求目标明确**   | 明确需求，如“优化逻辑”、“添加注释”、“只补全当前函数”     |
| R4   | **每次请求范围可控**   | 每次只作用于一个函数/模块，避免大段代码耦合                |
| R5   | **生成代码需人工复查** | 所有 AI 生成代码都必须人工审查                              |
| R6   | **禁止直接使用提交**   | AI 生成代码不得未经人工审核直接提交                        |
| R7   | **建议标注 AI 来源**   | 如有需要，可在注释中说明“由 AI 辅助生成，经人工审校”       |

---

## 🧠 使用习惯建议

| 场景             | 建议操作                                                       |
|------------------|----------------------------------------------------------------|
| 补全函数         | 在函数上方加注释，如 `# 获取用户数据并格式化为 JSON`          |
| 优化某段代码     | 明确选中 → 右键 AI → 提示“优化此 for 循环性能”                |
| 重构/模块化      | 拆分问题 → 分段让 AI 完成 → 人工审查整合                      |
| 插入库调用逻辑   | 显式告诉 AI 用哪个库（如 `requests`, `os`）避免自动猜测       |
| 命名风格一致性   | 明确提示“变量命名风格需与现有保持一致，如 camelCase”          |

---

## 📏 行为限制建议（团队协作时）

| 场景                              | 是否允许 | 说明                                       |
|-----------------------------------|----------|--------------------------------------------|
| 修改无关函数                      | ❌       | 限制 AI 操作范围，避免意外修改其他逻辑     |
| 补全整个类/页面                   | ✅       | 允许，但必须按模块逐一审查                 |
| 使用英文 prompt                   | ⭕️       | 可用，但团队建议统一使用中文               |
| 引入新第三方库                    | ⭕️       | 需注明用途并经人工审核                     |
| 重命名已有变量/函数               | ❌       | 非必要不可重命名，除非提示中特别说明       |
| 提交未经审查的 AI 代码            | ❌       | 禁止直接提交 AI 生成内容                   |

---

## ✅ VS Code 建议配置

- Copilot：关闭自动补全，改为手动触发（更可控）
- Cline AI：默认“选中范围后右键调用”，避免全局修改
- 启用 Git Hook：提交前自动运行 linter，防止非规范代码入库

---

## ✍️ 示例场景推荐写法

**❌ 错误：**
> Copilot 自动补全了整个模块，你没审查直接提交，影响其他模块逻辑。

**✅ 正确：**
> 精确选中函数 → 右键调用 AI → 提示“仅优化这段查询逻辑” → 人工审查后决定是否替换。

---



